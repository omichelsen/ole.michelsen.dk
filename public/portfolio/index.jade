ul.projects
	for item, slug in public.portfolio._data
		li
			a(href='/portfolio/#{ slug }')
				span.image
					img(src='#{ cdn }portfolio/#{ slug }/thumb.png', alt='#{ item.title }')
				span.text
					span.title= item.title
