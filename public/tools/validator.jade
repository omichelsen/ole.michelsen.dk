p
  | This tool validates a range of&nbsp;
  abbr(title='Uniform Resource Identifiers') URI
  | 's against the official&nbsp;
  a(href='http://validator.w3.org/docs/api.html', rel='external', target='_blank') W3C Markup Validation Web Service
  | . Simply enter the URI of your sitemap file, and this tool will automatically read and validate the linked pages from the file. The sitemap must be in the&nbsp;
  a(href='http://sitemaps.org/protocol.php', rel='external', target='_blank') Sitemap
  | &nbsp;or&nbsp;
  abbr(title='Really Simple Syndication') RSS
  | &nbsp;feed format.
p
  | In accordance with best practices for consuming remote resources, this tool will only process&nbsp;
  i one URI per second
  | &nbsp;so not to overload the validator web service. Furthermore it will not process more than 30 URI's. I will perhaps revise this cap later, when I see how it performs.
form#validator.c
  label(for='sitemapuri')
    b Sitemap URI:
  br
  input#sitemapuri.text(autofocus='autofocus', name='sitemapuri', required='required', style='width: 500px;', tabindex='0', type='url', value='http://ole.michelsen.dk/test/sitemap.xml')
  | &nbsp;
  input.button(type='submit', value='Validate')
h3 Validation results
table.tableFigure(style='width: 100%;')
  col(style='width: 40px;')
  col
  col(style='width: 80px;')
  thead
    tr
      th.r #
      th URI
      th.c Result
  tbody#results
    tr
      td.c(colspan='3')
        i Click "Validate" to process the sitemap

script(src="#{ libs.jquery }")
script(src="/scripts/validator.js")
